import { Routes } from '@angular/router';
import { PageComponentComponent } from './page-component/page-component.component';

export const routes: Routes = [
    {path: 'page-component', component: PageComponentComponent}
];
